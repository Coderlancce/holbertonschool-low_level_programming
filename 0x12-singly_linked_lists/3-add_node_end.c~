#include "lists.h"

/**
 *
 *
 */

list_t *add_node_end(list_t **head, const char *str)
{
  list_t *new, *temp;
  size_t size_char;

  new = malloc(sizeof(list_t));
  if (!new)
    return (NULL);

  new->str = strdup(str);
  new->len = _strlen(str);
}
