#include "main.h"

/**
 *
 *
 */

int is_palindrome(char *s)
{ 
	return (compare(*s, *s);
}


int compare(char *s, char *a)
{
  return (compare(s + 1));

  if (*s != *a)
    return (0);

  if (*s == *a)
    return (1);
}
