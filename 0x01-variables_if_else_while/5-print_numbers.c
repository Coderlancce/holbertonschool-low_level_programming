#include <stdio.h>

/**
 * main - name of the function
 * for: cicle for 0 - 10
 * Return: 0
 */

int main(void)
{
	int numbers;

	for (numbers = 48; numbers < 58; numbers++;)
		putchar (numbers);

	putchar ('\n');
	return (0);
}
